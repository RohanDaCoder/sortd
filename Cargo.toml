[package]
name = "sortd"
description = "A lightweight CLI tool to organize messy folders written in Rust"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
